class TestClass {
    // Class variable declarations
    static int count;
    field boolean isActive;
    field int x, y;
    field int[] numbers;

    // Constructor with local variables
    constructor TestClass new(int x, int y) {
        var int i, j, numbers;
        var boolean success;
        var int[] testArray;
    }

    // Function with var declarations
    // let statements
    // and an if statement
    function void doSomething(char name) {
        var char c;
        var int temp;

        let c = "H";
        let temp = 2 + (4 + 3);

        if(4 > 5){
          do Output.printString("Hello");
        }
    }

    // Method with no local vars
    method void reset() {
    }
}
