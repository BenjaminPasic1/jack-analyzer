class Main {
    function void main() {
        var int a, b, c;
        var Array arr;

        let a = 5;
        let b = 10;
        let c = a + b * (2 + 3);

        let arr = Array.new(3);
        let arr[0] = c;
        let arr[1] = a + b;
        let arr[2] = arr[0] + arr[1];

        if (arr[2] > 20) {
            do Output.printString("Greater than 20");
        } else {
            do Output.printString("Less or equal to 20");
        }

        while (a < b) {
            let a = a + 1;
            do Output.printInt(a);
        }

        do Output.printString("Done!");
        return;
    }
}
